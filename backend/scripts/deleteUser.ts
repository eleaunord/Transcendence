// import db from '../src/db/db';

// const emailToDelete = 'rym@test.com';

// const result = db.prepare('DELETE FROM users WHERE email = ?').run(emailToDelete);

// if (result.changes > 0) {
//   
// } else {
//  
// }
